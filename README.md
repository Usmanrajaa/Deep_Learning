GPT (decoder‑only) → generates text left‑to‑right

Seq2Seq + Attention (encoder‑decoder) → translates/reverses

Transformer for reverse (encoder‑decoder) → full self‑attention

NTM (memory augmented) → external memory

Vanilla RNN / LSTM → foundational sequence models
